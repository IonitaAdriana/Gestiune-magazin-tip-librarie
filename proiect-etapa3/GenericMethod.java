/*import datePersoane.Angajati;
import datePersoane.Client;
import Produse.Carte;
import Produse.Boardgame;


import java.util.ArrayList;

public class GenericMethod {

        public static ArrayList<Angajati> void printGenericAngajati(ArrayList<Angajati> inputArray ) {

            for(Angajati element : inputArray) {
                System.out.println(element.getNume() +" "+ element.getDepartament()+" "+ element.getSalariu());
            }
            System.out.println();
        }

    public static ArrayList<Client> void printGenericAngajati(ArrayList<Client> inputArray ) {

        for(Client element : inputArray) {
            System.out.println(element.getNume() +" " + element.getVarsta()+ " " +element.getNrPuncte());
        }
        System.out.println();
    }

    public static ArrayList<Client> void printGenericClienti(ArrayList<Client> inputArray ) {

        for(Client element : inputArray) {
            System.out.println(element.getNume() +" " + element.getVarsta()+ " " +element.getNrPuncte());
        }
        System.out.println();
    }

    public static ArrayList<Carte> void printGenericCarte(ArrayList<Carte> inputArray ) {

        for(Carte element : inputArray) {
            System.out.println(element.getTitlu() +" " + element.getAutor());
        }
        System.out.println();
    }

    public static ArrayList<Boardgame> void printGenericBoardgames(ArrayList<Boardgame> inputArray ) {

        for(Boardgame element : inputArray) {
            System.out.println(element.getTip() +" " + element.getDurata()+ " " +element.getVarsta());
        }
        System.out.println();
    }




}*/